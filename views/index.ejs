<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous" />
  <style>
    body {
      padding-top: 56px; /* navbar fixed-top 공간 확보 */
      background-color: #f8f9fa;
    }
    .banner img {
      width: 100%;
      max-height: 280px;
      object-fit: cover;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgb(0 0 0 / 0.1);
    }
    .product-card {
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 2px 6px rgb(0 0 0 / 0.15);
      background: white;
      transition: transform 0.2s ease-in-out;
    }
    .product-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 6px 12px rgb(0 0 0 / 0.2);
    }
    .product-card img {
      width: 100%;
      height: 180px;
      object-fit: cover;
    }
    .product-title {
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      font-weight: 600;
      margin-top: 10px;
      margin-bottom: 4px;
    }
    .product-price {
      font-weight: bold;
      color: #0d6efd; /* Bootstrap primary color */
      margin-bottom: 12px;
    }
  </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-light bg-white fixed-top shadow-sm">
  <div class="container">
    <a class="navbar-brand" href="/">🛍️ <%= title %></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav"
            aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item"><a href="/" class="nav-link active">홈</a></li>
        <li class="nav-item"><a href="/product" class="nav-link">상품목록</a></li>
        <li class="nav-item"><a href="/cart" class="nav-link">장바구니</a></li>
        <li class="nav-item"><a href="/board" class="nav-link">고객센터</a></li>
        <li class="nav-item"><a href="/user/login" class="nav-link">로그인</a></li>
      </ul>
    </div>
  </div>
</nav>

<div class="container my-4">

  <!-- 배너 -->
  <div class="banner mb-5">
    <img src="/images/7.jpg" alt="쇼핑몰 배너" />
  </div>

  <!-- 인기 상품 -->
  <h2 class="mb-4">🔥 인기 상품</h2>
  <div class="row row-cols-2 row-cols-md-3 row-cols-lg-4 g-4">
    <% products.forEach(product => { %>
      <div class="col">
        <div class="product-card shadow-sm h-100 d-flex flex-column">
          <img src="<%= product.image %>" alt="<%= product.name %>" />
          <div class="p-3 flex-grow-1 d-flex flex-column justify-content-between">
            <div>
              <h5 class="product-title"><%= product.name %></h5>
              <p class="product-price">₩ <%= product.price.toLocaleString() %></p>
            </div>
            <a href="/product/<%= product.id %>" class="btn btn-outline-primary btn-sm mt-auto">상세 보기</a>
          </div>
        </div>
      </div>
    <% }) %>
  </div>

</div>

<footer class="bg-white text-center py-3 mt-5 shadow-sm">
  <div class="container">
    <p class="mb-0">&copy; 2025 쇼핑몰 예시. All rights reserved.</p>
  </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
</body>
</html>