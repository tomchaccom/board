<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>로그인</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        body {
            padding: 2rem;
            background-color: #f8f9fa;
            display: flex;
            flex-direction: column; /* 세로 정렬을 위해 column 추가 */
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        .login-container {
            background-color: #fff;
            padding: 2rem;
            border-radius: 0.5rem;
            box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
            width: 100%;
            max-width: 400px;
        }
        /* 아이디/비밀번호 찾기 링크 버튼 스타일 조정 */
        .btn-link {
            font-size: 0.9rem; /* 폰트 크기 조정 */
            padding-top: 0;
            padding-bottom: 0;
        }
    </style>
</head>
<body>
<div class="login-container">
    <h2 class="text-center mb-4">로그인</h2>
    <% if (message) { %>
        <div class="alert alert-danger" role="alert">
            <%= message %>
        </div>
    <% } %>
    <form action="/user/login" method="post">
        <div class="mb-3">
            <label for="username" class="form-label">아이디</label>
            <input type="text" class="form-control" id="username" name="username" required>
        </div>
        <div class="mb-3">
            <label for="password" class="form-label">비밀번호</label>
            <input type="password" class="form-control" id="password" name="password" required>
        </div>
        <button type="submit" class="btn btn-primary w-100 mb-3">로그인</button>
    </form>
</div> <div class="d-flex justify-content-center mt-3">
    <a href="#" class="btn btn-link text-decoration-none mx-2" onclick="alert('아이디/비밀번호 찾기 기능은 구현 예정입니다.'); return false;">아이디/비밀번호 찾기</a>
    <span class="text-muted">|</span>
    <a href="/user/register" class="btn btn-link text-decoration-none mx-2">회원가입</a>
</div>

<div class="d-flex justify-content-center mt-3">
    <a href="/" class="btn btn-info btn-sm">홈으로</a>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>