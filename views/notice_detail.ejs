<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= notice.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        body {
            padding-top: 56px;
            background-color: #f8f9fa;
        }
        .container {
            margin-top: 2rem;
            background-color: #ffffff;
            padding: 2.5rem;
            border-radius: 0.5rem;
            box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
            max-width: 800px; /* 상세 페이지 너비 제한 */
        }
        .notice-header {
            border-bottom: 1px solid #dee2e6;
            padding-bottom: 1rem;
            margin-bottom: 1.5rem;
        }
        .notice-header h2 {
            font-size: 1.8rem;
            margin-bottom: 0.5rem;
            color: #343a40;
        }
        .notice-meta {
            font-size: 0.9rem;
            color: #6c757d;
        }
        .notice-content {
            min-height: 200px;
            padding-top: 1rem;
            white-space: pre-wrap; /* 내용에 줄바꿈 적용 */
            font-size: 1.1rem;
            line-height: 1.8;
            color: #495057;
        }
        .back-button-container {
            margin-top: 2rem;
            text-align: right;
        }
    </style>
</head>
<body>
<%- include('partials/navbar') %>

<div class="container">
    <div class="notice-header">
        <h2><%= notice.title %></h2>
        <p class="notice-meta">
            작성자: <%= notice.author %> |
            작성일: <%= notice.created_at.substring(0, 10) %> |
            조회수: <%= notice.views %>
        </p>
    </div>

    <div class="notice-content">
        <%= notice.content %>
    </div>

    <div class="back-button-container">
        <a href="/notice" class="btn btn-secondary">목록으로</a>
        <% if (user && user.isAdmin) { %>
        <a href="/notice/edit/<%= notice.id %>" class="btn btn-warning ms-2">수정</a>
        <form action="/notice/delete/<%= notice.id %>" method="post" class="d-inline ms-2" onsubmit="return confirm('정말로 이 공지사항을 삭제하시겠습니까?');">
            <button type="submit" class="btn btn-danger">삭제</button>
        </form>
        <% } %>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>